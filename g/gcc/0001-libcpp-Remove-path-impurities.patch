From 9f1c200942352dc8d165b507b98fe41a4fcecef0 Mon Sep 17 00:00:00 2001
From: "William A. Kennington III" <wak@google.com>
Date: Tue, 3 Jan 2017 19:25:17 -0800
Subject: [PATCH 1/3] libcpp: Remove path impurities

---
 libcpp/macro.c | 89 ++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
 1 file changed, 89 insertions(+)

diff --git a/libcpp/macro.c b/libcpp/macro.c
index c2515534504..13e54126c88 100644
--- a/libcpp/macro.c
+++ b/libcpp/macro.c
@@ -227,6 +227,87 @@ static const char * const monthnames[] =
   "Jul", "Aug", "Sep", "Oct", "Nov", "Dec"
 };
 
+char **split_path(cpp_reader *pfile, char *file) {
+	size_t num_separators = 2;
+	char **ret = NULL;
+	size_t i = 0;
+	size_t curr_part = 0;
+
+	for (i = 0; file[i] != '\0'; ++i)
+		if (file[i] == '/')
+			++num_separators;
+
+	ret = (char **)_cpp_unaligned_alloc(pfile, sizeof(char*) * num_separators);
+	ret[curr_part++] = file;
+	for (i = 0; file[i] != '\0'; ++i)
+		if (file[i] == '/') {
+			file[i] = '\0';
+			ret[curr_part++] = &file[i+1];
+		}
+	ret[curr_part] = NULL;
+
+	return ret;
+}
+
+char *copy_string(cpp_reader *pfile, const char *str) {
+	size_t str_len = strlen(str) + 1;
+	return strncpy((char *)_cpp_unaligned_alloc(pfile, sizeof(char) * str_len), str, str_len);
+}
+
+char *remove_build_top(cpp_reader *pfile, const char *file) {
+	const char *build_home = getenv("NIX_BUILD_TOP");
+	char *ret = copy_string(pfile, file);
+	size_t ret_pos = 0;
+	char **file_list = NULL;
+	ssize_t file_max = 0;
+	ssize_t file_pos = 0;
+	char *top_dir = NULL;
+	char **top_list = NULL;
+	ssize_t top_pos = 0;
+	ssize_t top_max = 0;
+
+	if (build_home == NULL)
+		goto no_home;
+
+	top_dir = copy_string(pfile, build_home);
+	file_list = split_path(pfile, ret);
+	top_list = split_path(pfile, top_dir);
+
+	for (; file_list[file_pos+1] != NULL; ++file_pos);
+	file_max = file_pos;
+	for (; top_list[top_pos+1] != NULL; ++top_pos);
+	top_max = top_pos;
+
+	for (; file_pos >= 0; --file_pos) {
+		char *fn = file_list[file_pos];
+		if ((file_pos != 0 && fn[0] == '\0')
+			|| strcmp(".", fn) == 0) {
+			file_list[file_pos] = NULL;
+		} else if (top_pos < top_max && strcmp("..", fn) == 0) {
+			file_list[file_pos] = NULL;
+			++top_pos;
+		} else if (top_pos >= 0 && strcmp(fn, top_list[top_pos]) == 0) {
+			file_list[file_pos] = NULL;
+			--top_pos;
+		}
+	}
+
+	for (file_pos = 0; file_pos <= file_max; ++file_pos) {
+		char *fn = file_list[file_pos];
+		size_t inner_pos = 0;
+		if (fn == NULL)
+			continue;
+		for (; fn[inner_pos] != '\0'; ++inner_pos)
+			ret[ret_pos++] = fn[inner_pos];
+		if (file_pos != file_max)
+			ret[ret_pos++] = '/';
+	}
+	ret[ret_pos] = '\0';
+
+no_home:
+	return ret;
+}
+
 /* Helper function for builtin_macro.  Returns the text generated by
    a builtin macro. */
 const uchar *
@@ -235,6 +316,12 @@ _cpp_builtin_macro_text (cpp_reader *pfile, cpp_hashnode *node,
 {
   const uchar *result = NULL;
   linenum_type number = 1;
+  bool enforce_purity = false;
+  const char *enforce_purity_str = getenv("NIX_ENFORCE_PURITY");
+  const char *build_top = getenv("NIX_BUILD_TOP");
+
+  enforce_purity = enforce_purity_str != NULL
+      && strcmp("1", enforce_purity_str) == 0;
 
   switch (node->value.builtin)
     {
@@ -301,6 +388,8 @@ _cpp_builtin_macro_text (cpp_reader *pfile, cpp_hashnode *node,
 	    if (!name)
 	      abort ();
 	  }
+	if (enforce_purity)
+	  name = remove_build_top(pfile, name);
 	len = strlen (name);
 	buf = _cpp_unaligned_alloc (pfile, len * 2 + 3);
 	result = buf;
-- 
2.11.0

